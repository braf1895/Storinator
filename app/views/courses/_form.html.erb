<%= form_with(model: course, local: true) do |form| %>
  <% if course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id, id: :course_course_id %>
  </div>

  <div class="field">
    <%= form.label :course_Description %>
    <%= form.text_field :course_Description, id: :course_course_Description %>
  </div>

  <div class="field">
    <%= form.label :number_credits %>
    <%= form.number_field :number_credits, id: :course_number_credits %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
